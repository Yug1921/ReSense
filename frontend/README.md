# Frontend — not started yet

Frontend tracks (E: Upload/Landing, F: Summary Panel, G: Visualization Panel,
H: Q&A Chat Panel) are intentionally not scaffolded yet — they're pending
your design references so the visual identity gets set once, correctly,
rather than rebuilt after a redesign.
